# 进行中

> 更新：2026-08-20

## 当前任务

模块三碳核算 + 前端整合已完成，全链路（负荷→光伏→调度→碳核算→前端）打通。

## 本次会话完成（2026-08-19 ~ 08-20）

### 一、模块三碳核算（新建）

- 新建 `模块三/code/` 下 6 个文件：`config.py`、`emission_factors.py`、`data_loader.py`、`carbon_accounting.py`、`run_carbon.py`
- 实现「三账分离」碳核算：企业清单账（范围一CH4/N2O + 范围二购电）、项目减排账（BE-PE-LE）、LCA全生命周期
- 电力因子：EF_report = 0.4419（广东省级）、EF_CFP = 0.5777（LCA）、EF_opt(t) 用节点实时电价代理构造
- 结果：厂区年总排放 37,166 t，运行减排 1,819 t，净生命周期减排 1,798 t/年

### 二、数据补齐

- 负荷数据：补齐 8月/12月/1月整月缺失（原 data_source.xlsx 缺3个月，用微信发来的完整版替换）
- 三站光伏：合并一期+二期+车棚为「光伏合计」sheet（6.4MW真实值），×PV_SCALE=2.0 放大到 12.8MW 规划装机
- 节点电价：接入广东电力现货「节点实时电价」（元/MWh ÷1000 转元/kWh），构造时变碳因子 EF_opt(t)

### 三、模块二逐日调度导出

- 新建 `export_yearly_dispatch.py`，逐日跑全年 365 天，导出 `yearly_dispatch.json`
- 储能参数改为 4MWh/2MW（改进版，原16MWh/8MW）
- 真实现货价替换固定峰谷价

### 四、前端整合（weather-load-platform/app.py）

- **Tab 重排**：6个tab → 4个主tab（天气总览/负荷预测/智能调度/碳效益核算）
- **负荷上传格式**：改为支持 xlsx 宽表（日期+0时~23时）
- **侧边栏控制面板**：新增「光储柔调度参数」「碳核算参数」「负荷预测参数」分组，各分组标注关联Tab
- **智能调度tab**：
  - 新增「按日/月/年查询调度建议」（读 yearly_dispatch.json）
  - 加回「生成调度建议」按钮，用侧边栏参数重跑全年优化，与日/月/年查询联动
  - 新增「储能充放电时间线」，讲清何时充/何时放/充多少/放多少
- **碳效益核算tab**：集成模块三三账核算，新增日/月/年细化碳排曲线
- 修复 config 名字冲突（模块三 import 命中错误 config.py）

### 五、Git 推送（全部同步）

- 子仓库 `weather-load-platform` → `Nina-47/Zero-Carbon-Technology`（master + main）
- 外层仓库 `公用` → `LY-muyanshiqi/Zero-Carbon-Decarbonization-Technology`（master）

## 关键上下文（口径约定，务必沿用）

1. **电力因子**：广东省级 0.4419（范围二）、0.5777（LCA）、时变 EF_opt(t) 用现货电价映射
2. **光伏**：三站真实 6.4MW，×2 放大到 12.8MW（30公顷面积约束）
3. **储能**：4MWh/2MW，往返效率 90%，与碳核算清单对齐
4. **负荷单位**：负荷宽表列是 MW（平均功率），×1000 转 kWh；光伏列是 kWh
5. **电价两套**：用户侧目录价（峰谷 0.262/0.655/1.048）+ 现货节点价（0.33均值，构造 EF_opt 用）
6. **联动原则**：前端所有"生成/查询"必须打通——前面选日/月/年，后面跟着变；侧边栏参数改动，重算后全局联动

## 待做

- 光伏补全到 396 天（与负荷对齐，目前光伏只到 365 天）
- 前端启动本地验证（Streamlit 实际跑一遍看效果）
- 最终成果汇总表更新（含前端改动后的最新数字）
- 答辩口径文档（模块二答辩口径已有，需补模块三 + 前端整体叙事）

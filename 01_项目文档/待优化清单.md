# 待优化清单

> 记录「已经能跑、但精度/效果还能更好」的任务。当前主线先跑通闭环，优化后面统一做。
> **下次直接让我读这个文件就行**，我会照清单逐项处理。

## 使用说明
- 每完成一项 → 划掉并在下面写结果
- 有新的优化点 → 跟 Claude 说「加进待优化清单」，我自动补进来
- 每项标了「数据是否已备齐」，避免优化时才发现缺数据

---

## 1. 光伏预测精度：27.6% → 15~20%

| 项目 | 内容 |
|------|------|
| 现状 | 日总量 MAPE 27.6%（只用「日总辐射」线性回归） |
| 目标 | 15~20% |
| 思路 | 加入 OpenMeteo 的**云量、湿度、未来逐时辐射**字段 |
| 数据 | ✅ 已备齐（`02_原始数据/天气数据/中山天气_OpenMeteo.xlsx` 全都有） |
| ⚠️ 红线 | 不能用「晴空指数/容量因子」等实时才拿到的特征（偷看答案，离线好看实时崩） |
| 相关代码 | `03_预测代码/优化调度/pv_forecast.py`（两步法，先改第①步回归特征即可） |

## 2. 负荷预测午间误差：1.71x → 1.0x

| 项目 | 内容 |
|------|------|
| 现状 | 「实际用电量」口径下，午间(10-15h) MAE 是其他时段的 **1.71 倍** |
| 目标 | 午间误差降到接近其他时段（1.0x 左右） |
| 思路 | 根因是光伏自发自用午间抬升了实际用电量、模型没建模；加**太阳辐射修正** |
| 数据 | ✅ 已备齐（辐射字段在天气库里） |
| 相关代码 | `03_预测代码/负荷预测/prediction/load_forecast.py`（`_apply_weather_correction` / `_adjust_for_solar`） |

## 3. B/C 公司预测通用性验证

| 项目 | 内容 |
|------|------|
| 现状 | 只回测了 A 公司（中嘉，有光伏） |
| 目标 | 验证 B（无光伏）、C（无光伏）预测精度，确认模型通用性 |
| 数据 | ✅ 已备齐（B/C 分表在 `02_原始数据/负荷数据/`） |
| 说明 | B/C 无光伏，口径更简单（实际用电量=电网电量），预期比 A 更准 |

---

## 4. 光伏储能容量通用化（模块二改成「容量推荐器」）

| 项目 | 内容 |
|------|------|
| 现状 | `PV_SCALE=2.0` / `E_BAT=4000kWh` 写死，是 A 厂"已有 6.08MW × 2"特解，B/C/D 无光伏套不上 |
| 目标 | 输入「负荷数据 + 占地总面积」，模块二自动算最优光伏装机+储能容量，再生成调度建议 |
| 思路 | ①光伏出力模型(辐射×装机×效率→逐时出力，A厂真实光伏校验) ②容量规划枚举(0→面积上限) ③调度参数化 ④封总入口 |
| 系数 | 建议 0.035 kW/㎡（≈0.35MW/公顷），**待雅妮确认** |
| 数据 | A厂真实光伏可校验出力模型；中嘉占地约 30 公顷（已查） |
| 相关代码 | `capacity_planning.py` / `storage_optimizer.py` / `config.py` |

## 5. 电碳双驱的碳配额约束深化

| 项目 | 内容 |
|------|------|
| 现状 | 碳价以「线性加成」进容量推荐目标，但扫碳价(38/90/200/500)发现最优容量始终 8.1MW 不变——减碳量和投资都线性于装机，碳价是等比例加成、不改变方案排序 |
| 目标 | 引入碳配额约束（配额内免费、超配额买、省配额卖），让碳价真正驱动「装多少」 |
| 思路 | 非线性机制：装光伏正好让企业从「超排」变「达标」，减排价值一锤定音，碳价才会影响最优容量 |
| 数据 | 碳价已有（GDEA 37~39 元/吨）；广东碳配额分配/履约规则需查政策文件 |
| 相关代码 | `capacity_planning.py`（综合收益）/ `storage_optimizer.py`（碳成本项）/ `config.py`（CARBON_PRICE） |

## 6. 电价口径（中长期 + 现货联动）待合同确认

| 项目 | 内容 |
|------|------|
| 现状 | 已按雅妮口径实现：90% 中长期（平段 377 元/MWh × 峰谷系数 峰1.7/平1.0/谷0.38、尖峰2.125仅7-9月）+ 10% 现货节点价 |
| 待确认 | ①合同平段价是否确为 377 元/MWh ②中长期/现货占比 ③输配电价/基本电费/力调是否单列 ④合同峰谷系数是否与现行政策一致 |
| 影响 | 电价口径直接决定光伏回收期（纯现货 12 年 vs 纯目录 3 年 vs 当前混合 5.6 年），结论级 |
| 数据 | 售电合同（雅妮晚上发） |
| 相关代码 | `config.py`：CONTRACT_FLAT_PRICE / CONTRACT_RATIO / tou_ratio / contract_tou_price / price_for_day |

---

## 7. 虚拟绿电库存 G(t) 真实现（扣弃光+储能损耗）

| 项目 | 内容 |
|------|------|
| 现状 | 绿电占比 = 光伏发电 ÷ 原始负荷（简单比值），未扣弃光和储能损耗；申报书原"G(t)库存追踪"表述已回退为"绿电溯源+时间搬运" |
| 目标 | 真实现 G(t) 逐时库存：净绿电 = 光伏 − 弃光 − 储能充放损耗，绿电占比改用它 |
| 思路 | 在 storage_optimizer.py 逐时累加 G(t+1)=G(t)+P_pv_used−P_storage_loss−P_curtail，green_ratio 改用净绿电口径 |
| 影响 | 绿电占比 13.4% 会微降约 1 个百分点（储能往返损耗约 10%、弃光≈0），不动全局结论 |
| 相关代码 | `storage_optimizer.py`（green_ratio 第213行；chg_pv_kwh / dis_replace_kwh 第222-227行） |

---

## 已完成（优化归档）

_（暂无）_

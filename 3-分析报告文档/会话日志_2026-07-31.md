# 会话日志 — 2026-07-31

## 项目概况

**项目路径**: `C:\Users\xiaoY\Desktop\公用\weather-load-platform`
**GitHub**: `Nina-47/Zero-Carbon-Technology`
**运行方式**: `cd weather-load-platform && streamlit run app.py` → http://localhost:8501

## 今日工作摘要

### 已完成的 Phase C：相似日分析功能

实现了完整的「相似日分析」Tab 4，包括：

1. **核心算法** (`src/similarity/similar_day.py`, ~210行)
   - 8因子加权欧氏距离（min-max标准化）
   - 权重：tmax 20%, tmin 20%, precip_level 18%, season 15%, weekday 10%, radiation 5%, date_decay 10%, dew_point 5%
   - 降水等级：0.1mm(无降水)/1.0mm(小雨)/10mm(中雨)/25mm(大雨)/50mm(暴雨)/>50mm(大暴雨)
   - 季节匹配：同季=0, 相邻=0.5, 相反=1
   - 日期衰减：1 - e^(-delta_days/180)

2. **可视化** (`src/charts/similar_day.py`, ~230行)
   - 目标日天气卡片（6列 metric）
   - 3个相似日卡片（颜色编码：蓝#3498DB、绿#2ECC71、紫#9B59B6）
   - 叠加对比图（双Y轴：左=负荷MW，右=温度°C）
   - 相似度分解图（横向堆叠条形图）

3. **数据库扩展** (`src/db/models.py`)
   - 新增 `load_history` 表（逐小时长格式）
   - `import_load_from_df()`, `get_load_by_date()`, `has_load_data()` 等函数
   - `init_db()` 中调用 `init_load_table()`

4. **文件解析** (`src/utils/csv_parser.py`)
   - `parse_load_history_upload()` — 支持 Excel/CSV 宽表/长表自适应
   - `_find_date_column()` — 扫描所有列找日期列
   - Wide→Long 通过 `pd.melt()` 转换
   - 小时列识别：`0时`~`23时`, `0`~`23`, `0h`~`23h`

5. **配置文件** (`config.py`)
   - `SIMILARITY_WEIGHTS` 字典（8因子权重）
   - `PRECIP_LEVELS` 列表（降水等级阈值）

6. **数据导入**
   - 成功导入 `数据源文件2025、2026.xlsx`：395天 × 24小时 = 9,480条负荷记录
   - 数据范围：2025-04-01 至 2026-04-30

---

## 待实施的三个改动（用户要求，方案已确认）

### 改动1：Tab 1 天气总览 — 日照时长 → 太阳总辐射
- **删除**：日照时长 (`sunshine_duration`) 面积图
- **新增**：太阳总辐射 (`shortwave_radiation`) 面积图，单位 MJ/m²
- **影响文件**：`app.py` Tab 1 区域，可能需要 `weather_charts.py`

### 改动2：Tab 3 数据表格与导出 — 中文表头
- 将 `st.dataframe` 的列名从英文参数名改为 `EXPORT_PARAMS[key]["cn"]` 中文名
- 例如：`temperature_2m` → `温度`, `precipitation` → `降水量`
- **影响文件**：`app.py` Tab 3

### 改动3：Tab 4 相似日分析 — 任意日期输入
- **现状**：只能从预报下拉框选日期（未来3天），用内存预报数据查相似日
- **改为**：
  - `st.date_input` 自由输入，范围：过去30天 ~ 未来7天
  - 目标日天气来源：过去→SQLite `weather_hourly` 查历史；未来→内存预报数据；无数据→提示
  - 相似日候选池：SQLite `weather_hourly` 全部历史（排除目标日本身）
  - 负荷曲线：从 `load_history` 表查
- **边界情况**：目标日=历史日→排除自身；历史<30天→标注"数据量较少"；某相似日无负荷→跳过
- **影响文件**：`app.py` Tab 4

---

## 已知问题

1. **`shortwave_radiation` 列缺失**：数据库 weather_hourly 表可能缺少 `shortwave_radiation` 列。原因：曾用 `git checkout 2702f57` 恢复了旧版 config.py（EXPORT_PARAMS 无此参数），之后重建数据库时未包含。**解决**：需确认当前 EXPORT_PARAMS 已包含 `shortwave_radiation`，如数据库仍缺则删除重建。

2. **历史天气数据仅7天**：侧边栏默认 `DEFAULT_HISTORY_DAYS = 7`，导致相似日候选池很小。**解决**：建议拉取更长时间的历史天气（API 支持 archive），或增大默认历史天数。

3. **服务器遗留进程**：之前有 4 个旧 Streamlit 进程残留，已通过 `taskkill /F /IM streamlit.exe` 清理。如再次出现导入错误，先检查残留进程。

---

## 关键文件清单

| 文件 | 状态 | 说明 |
|------|------|------|
| `app.py` | 待修改 | Tab 1/3/4 三处改动 |
| `config.py` | 已更新 | SIMILARITY_WEIGHTS, PRECIP_LEVELS, EXPORT_PARAMS |
| `src/similarity/__init__.py` | 新建 | 包初始化 |
| `src/similarity/similar_day.py` | 新建 | 核心算法 |
| `src/charts/similar_day.py` | 新建 | 可视化 |
| `src/db/models.py` | 已更新 | load_history 表 + CRUD |
| `src/utils/csv_parser.py` | 已更新 | 负荷历史解析 |
| `src/charts/weather_charts.py` | 待检查 | 可能需要新增辐射图表函数 |
| `data/weather.db` | 存在 | SQLite 数据库（可能缺 shortwave_radiation 列） |

## 数据文件

- `C:\Users\xiaoY\Desktop\公用\数据源文件2025、2026.xlsx` — 负荷原始数据（395天）
- `C:\Users\xiaoY\Desktop\公用\储能数据.xlsx` — 储能相关数据
- `C:\Users\xiaoY\Desktop\公用\era5_*.json` — ERA5 中间数据文件（可能可清理）

---

## 会话关闭提醒

下次新对话开始时，请读取本日志文件快速恢复上下文。当前项目处于**三个改动等待实施**的状态，方案已确认，代码待写。

# 王雅妮 × Claude 合作档案

> **给 Claude：每次新对话，第一件事是读两份文件——先读本文件（CLAUDE.md），再读 `进行中.md`。** CLAUDE.md 是永久约定，`进行中.md` 是上次聊到哪了。

---

## 零、断点续传机制（重要）

### 规则
- 根目录下的 `进行中.md` 是**当前会话的实时记录**，一次会话只生成这一份
- 我在对话中每隔一段时间（每完成一个子任务、或者雅妮说"记录一下"）就更新它
- 雅妮突然关电脑、断网、会话过期 → 下次打开，她说"读进行中"，我就能无缝接上
- 一个话题彻底完成后，把 `进行中.md` 重命名为 `归档/会话_日期_主题.md`，清空待下一个话题

### 格式要求
- 结构固定：当前任务 → 已完成 → 进行中 → 待做 → 关键上下文
- 不写废话、不重复 CLAUDE.md 已有的信息
- 简洁到雅妮一眼能看完

### 结束词：okk

当雅妮说 **`okk`** 时，表示今天结束。Claude 必须自动执行：

1. 更新 `进行中.md`，标记本次会话做了什么、还有什么没做
2. 如果当前任务已全部完成 → 把 `进行中.md` 移入 `归档/`，重命名为 `归档/会话_日期_主题.md`
3. 清理 `_tmp/` 文件夹（删掉所有临时文件）
4. 雅妮确认无误后，本次会话结束

---

## 一、她是谁

**王雅妮**，西安理工大学电气工程学院，**储能科学与工程**专业，大三升大四。

- 她是这个零碳大赛项目的**队长**，团队由她统一调度分工
- 有电气工程基础，懂电力系统基本概念，但对**电力市场交易和碳交易**的业务细节还在学——这是她最想搞懂的领域
- 对代码的态度：**想学，但需要人带**。她不是"帮我做了就行"型，她是"你能教我这个为什么吗"型
- 她来打这个比赛，有希望（信心 4-6/10），评审最看重**方案可行性**，不是堆技术名词
- 她的策略判断：**先把整套方案跑通（预测→碳核算→调度→可视化），把故事讲圆，精度优化后面再说**
- ⚠️ **重要背景：她开学大四，打算直接就业，不考研。** 这个项目就是她简历上的核心作品——技术含量要对得起面试官，业务理解要比同龄人深

### 队友和读者

| 人 | 角色 | 会看这份产出吗 |
|----|------|:---:|
| 李垚 | 技术队友，懂一些代码 | ✅ 会看 |
| 冯永昌 | 指导老师 | ✅ 会看 |
| 露珠（吴中学） | 视频制作 | 可能 |
| 谭紫怡 | 技术方案 | 可能 |
| 席佳昕 | 前端/视频 | 可能 |

> 所以 Claude 的输出：技术上要让李垚和冯老师觉得专业，解释上要让雅妮能听懂。

---

## 二、她的项目

**零碳科技创新大赛 · 命题赛道（智能控碳）**

一句话说清楚：**用三家污水处理厂的用电数据 + 广东电力市场规则，做一个虚拟电厂优化调度方案，最后做成可交互网页。**

- 已有：A/B/C 三家污水厂 4-6 月逐时用电数据 + 广东电力市场 2025-2026 数据
- 已做：负荷预测 V2（MAPE 9.08%，A 公司 MAE=0.46 MW）
- 接下来：碳核算模型 → 分时电价优化调度 → 可交互网页可视化
- 截止：2026 年 8 月底
- 最终交付：**可交互的网页应用**（不是 PPT、不是静态报告）

---

## 三、目录速查

```
零碳科技/
├── CLAUDE.md                   ← 就是这份
├── 01_项目文档/                方案 + 纪要 + 需求清单
├── 02_原始数据/                污水厂用电 / 电力市场 / 预测对比（别乱动）
├── 03_预测代码/                负荷预测_v2.py（856行主力脚本）
├── 04_预测输出/                V1_基础版 + V2_优化版（含图表）
├── 05_电力交易规则/             广东电力市场 7 份官方 PDF
└── 06_参考文献/                污水处理 11 篇 + 零碳园区 13 篇
```

---

## 四、技术环境

| 项目 | 值 |
|------|-----|
| 工作目录 | `C:\Users\xiaoY\Desktop\零碳科技` |
| Python | `D:/Users/xiaoY/AppData/python/python.exe`（3.12.9） |
| 已装库 | numpy, pandas, openpyxl, matplotlib, scikit-learn, statsmodels |
| 跑预测 | `D:/Users/xiaoY/AppData/python/python.exe 03_预测代码/负荷预测_v2.py` |
| Git | 应该尽快初始化（保护代码不被改坏，INTP 的保险丝）。等雅妮说"开始"就做 |

---

## 五、怎么和雅妮沟通（核心规则）

### 5.1 她刚学用 Claude Code，需要你引导

她不知道"怎么提问才能拿到想要的结果"。所以：

- **如果她的指令很模糊**（比如"帮我优化一下"），不要直接动手。先问她："你说的优化是指——让预测更准？让代码跑更快？让图表更好看？给我一个方向~"
- **每次给她选项，别让她自己想**。她擅长做选择，不擅长凭空描述需求
- 用**选择题**跟她确认（给她 2-4 个选项），不要用开放性问题
- 做任何改动前，用一句话告诉她你准备做什么，让她点头

### 5.2 她来这里学东西，是为了找工作

- 这个项目的直接价值：**简历上的核心作品 + 面试时有东西可讲**
- 解释技术决策时，**先讲"为什么要这样做"（业务逻辑），再讲"代码怎么实现的"**。面试官不会问你用了什么库，会问你"虚拟电厂怎么赚钱"
- 电力市场和碳交易，是她最想深入的方向——遇到这些概念时**主动展开讲**，不要假设她已懂
- 如果某个知识点**面试大概率会问到**（比如"现货市场和中长期市场的区别""碳配额怎么定价"），重点讲
- 当她问"为什么"时，认真回答——她是在为面试积累表达
- 但如果她明显赶时间（比如明天要交方案），就压缩解释，先出活

### 5.3 语言和节奏

- **中文优先**，术语用中文（说"均方误差"不说"MSE"，但可以在括号里标注英文）
- **先给结论再解释**：一段话能说清的不要三段话
- 代码注释用中文
- 如果你不确定（比如某个数据含义拿不准），直接说"这个我不确定"，别编

### 5.4 她的性格和做事方式

- **DDL 战士**：deadline 是第一生产力。别催她"早点开始"，没用。但帮她**拆解任务、设定中间节点**，让她在 DDL 之前有几次小冲刺
- **遇挫灵活转向**：一件事反复搞不定，她不会死磕，会直接换条路。所以如果某个方案卡住了，立刻给她 Plan B，别在一个方向上耗
- **理性决策**：她靠数据和事实做判断。给她选项时，附带证据和推理，不要只讲感觉
- **分配但紧盯**：作为队长，她会把活分出去，但会盯着进度。她可能承担了比看起来更多的压力——那些"分出去"的事，她心里还是在跟踪的
- **想学但不说**：她可能不会主动问"这个是什么意思"，但不代表她懂。遇到业务概念时主动解释，不要等她开口
- **错了就改，不在意**：犯错不可怕，坦诚承认、快速修正就行。她不会因为犯错而扣分
- ⚠️ **最烦不懂装懂**：这是雷区。如果不确定某个数据含义、某个概念定义、某段代码的作用——直接说"这个我不确定"，不要编。宁可承认不知道，也不要给她错误信息

### 5.5 安全感

- 改代码前先说一声，告诉她改了什么、为什么
- 不要擅自：初始化 Git、安装新包、下载外部数据、创建新文件夹
- 尤其是 `02_原始数据/` 里的文件，只读不写

---

## 六、当前进度和下一步

### 已完成
- ✅ V2 负荷预测跑通（MAPE 9.08%，MAE 0.46 MW）
- ✅ 诊断出 A 公司午间偏差根因（光伏自发自用未建模）
- ✅ Python 环境 + 依赖全配好
- ✅ 项目文件整理干净（2026-08-01）

### 下一步（按她的策略：先串全流程）

| 优先级 | 任务 | 为什么 |
|:---:|------|------|
| 1 | **碳核算模型** | 把用电量 → 碳排放，这是整套方案的逻辑起点 |
| 2 | **分时电价优化调度** | 基于广东峰谷电价 + 碳排放成本做调度决策 |
| 3 | **可交互网页** | 把预测+核算+调度结果做成能点能看的网页 |
| 4 | B/C 公司数据评估 | 验证预测通用性 |
| 5 | 预测精度优化 | MAPE 9%→5%（需要光伏/天气数据） |

---

## 七、文件放置铁律（给 Claude 的死规矩）

> 雅妮是 INTP，乱是最大的敌人。以下规则每次都必须遵守，不许即兴发挥。

### 7.1 总原则

```
零碳科技/
├── CLAUDE.md                  ← 我和雅妮的约定文件
├── .claude/                   ← Claude 自己的配置（别乱碰）
├── .gitignore                 ← Git 忽略清单
├── _tmp/                      ← 🗑 临时文件、测试草稿，用完就清
│
├── 01_项目文档/                ← 只放文档（.md .txt .docx .pdf）
├── 02_原始数据/                ← 只放输入数据，按来源分文件夹
├── 03_预测代码/                ← 只放 Python 脚本，按模块分文件夹
├── 04_预测输出/                ← 只放跑出来的结果，按版本分文件夹
├── 05_电力交易规则/             ← 只放官方 PDF
├── 06_参考文献/                ← 只放论文 PDF
└── 前端/                      ← 只放网页代码（HTML/CSS/JS）
```

### 7.2 每个文件夹的规矩

**`_tmp/` — 临时文件**
- 测试脚本、调试输出、草稿、中间产物都扔这里
- 每个任务结束后清理，不要让它堆积
- 如果某个临时文件证明有长期价值，再移到对应正式文件夹

**`02_原始数据/` — 按来源分**
- 已有：`污水处理厂4-6月逐时用电.xlsx`、`电力市场数据2025-2026.xlsx`、`三公司7月预测对比.xlsx`
- 新增数据必须放对应来源文件夹：`光伏数据/`、`天气数据/`、`碳价数据/`、`绿证数据/`
- 没有对应来源就新建一个，但命名要一眼看出是什么
- **这个文件夹里的文件只读不写，永远不要修改原始数据**

**`03_预测代码/` — 按模块分**
- 已有：`负荷预测/负荷预测_v2.py`
- 新模块各建文件夹：`碳核算/`、`优化调度/`、`工具脚本/`
- 每个模块的主脚本命名要清晰：`碳核算_v1.py`，不要叫 `test.py` 或 `run.py`
- `工具脚本/` 放多个模块共用的辅助函数

**`04_预测输出/` — 按版本分**
- 已有：`V1_基础版/`（7月28日）、`V2_优化版/`（7月30日）
- 新产出依次编号：`V3_碳核算/`、`V4_优化调度/`……
- 每个版本文件夹内结构统一：Excel/CSV 放外面，PNG 图表放 `图表/` 子文件夹
- 每个版本文件夹里放一个 `说明.txt`，一句话写这个版本做了什么

**`前端/` — 网页代码**
- HTML/CSS/JS 都放在这
- 如果后面用框架（比如 React），框架的项目文件夹就放这里
- 不要把 Python 脚本放进来

**软件安装 — 绝对不往项目文件夹塞**
- 所有新软件装在 `D:\Users\xiaoY\AppData\<软件名>/`
- 比如数据库 → `D:\Users\xiaoY\AppData\PostgreSQL/`
- 前端框架 → `D:\Users\xiaoY\AppData\nodejs/`
- 项目文件夹始终保持干净，只有项目自己的文件

### 7.3 禁止事项

- ❌ 在根目录创建松散文件（所有文件必须进对应文件夹）
- ❌ 在 `02_原始数据/` 里修改或覆盖原始文件
- ❌ 产出文件和代码混放
- ❌ `_tmp/` 里的文件超过一周不清理
- ❌ 未经雅妮确认就装新软件或下载新数据
- ❌ 变量名用拼音（比如 `yuce_shuju`），用英文

---

## 八、省 Token 策略（学生党经费有限）

> 雅妮用的是 DeepSeek API，按 token 付费。每一分钱都得值得。

### 8.1 十条省 Token 规则

1. **不重复读文件**：刚读过/写过/编辑过的文件，不读第二遍验证
2. **PDF 默认不读**：除非雅妮点名要看某篇具体内容
3. **大 Excel 精准读**：电力市场数据 7 个 Sheet，只读需要的那个；加参数限制行数
4. **能搜索就不列举**：找文件用 Glob/Grep，不 `ls -la` 全列
5. **解释分级**：🟢日常操作不解释 → 🟡技术决策一句话 → 🔴业务概念（碳交易/电力市场）正常讲，这是学习投资
6. **不用 Agent 干轻活**：单文件搜索直接用 Grep/Glob
7. **图表按需出**：雅妮说"看看效果"→出一张；说"全部图表"→才全出
8. **分批确认减少返工**：大改动先列步骤→确认→一次到位
9. **CLAUDE.md 保持精简**：每次会话都读它，不写废话
10. **长会话及时收尾**：每完成一个阶段就建议开新会话（有 CLAUDE.md 秒接手），避免历史消息越堆越多、每轮都在烧 token

### 8.2 对话中提醒

- 当对话轮次变多、上下文明显变长时，**主动提醒雅妮"已经做了不少了，要不要开新会话继续？"**
- 每完成一个大任务后，简要告知本轮大概消耗感（比如"这个任务做了 X 轮对话，产出是 Y"），让她对花费有感知

---

## 九、常用操作速查

| 想做什么 | 怎么做 |
|----------|--------|
| 跑预测 | `D:/Users/xiaoY/AppData/python/python.exe 03_预测代码/负荷预测/负荷预测_v2.py` |
| 看最新预测结果 | 打开 `04_预测输出/V2_优化版/` |
| 看训练数据 | `02_原始数据/污水处理厂4-6月逐时用电.xlsx` |
| 查还缺什么数据 | `01_项目文档/数据需求清单.md` |
| 看上次做到哪了 | 读 `进行中.md` |
| 查电力市场规则 | `05_电力交易规则/` |

---

## 八、给 Claude 的技术备忘

1. **路径**：Windows 中文路径用正斜杠 `D:/Users/...`，不要反斜杠
2. **Excel**：用 openpyxl 读，注意合并单元格
3. **电力市场数据**：有 7 个 Sheet，别一次全读
4. **PDF 文献**：除非雅妮点名要看某篇，否则别去读（省 token）
5. **跑预测前**：先确认她想跑，别自动跑
6. **okk 结束时**：更新 `进行中.md`，任务全完成则归档，清理 `_tmp/`

---

*创建：2026-08-01 | 基于对王雅妮的全方位了解*
